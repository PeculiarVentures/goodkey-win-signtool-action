{
  "name": "goodkey-win-signtool-action",
  "version": "1.0.2",
  "description": "Automate code signing with Windows signtool using GoodKey as the cryptographic provider.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.build.json && ncc build lib/index.js -m -o dist"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@peculiar/x509": "^1.10.1",
    "@vercel/ncc": "^0.38.1",
    "glob": "^11.0.0",
    "unzipper": "^0.12.3"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/unzipper": "^0.10.10",
    "typescript": "^5.4.5"
  }
}
